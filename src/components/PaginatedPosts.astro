---
import PaginatedDefaultPosts from "./PaginatedPosts/Default.astro";

const { tag, years, target } = Astro.props;
---

{
  (() => {
    switch (tag) {
      default:
        return <PaginatedDefaultPosts tag={tag} years={years} target={target} />;
    }
  })()
}