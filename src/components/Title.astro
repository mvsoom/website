---
import Monospan from "../components/Monospan.astro";

const { title } = Astro.props;

/*
The title link should just refer to the current page, except if:
- ends in a year, eg. /media/2024
- is part of a series, eg. /about/website/3
In both cases it should link to the parent page, eg. to /media and /about/website in previous examples.
*/
let href = Astro.url.pathname;
const regex = /\/\d+$/;
if (regex.test(href)) {
  href = href.replace(regex, "");
}
---

<div class="title">
  <!-- Wrap in h2 as temporary hack -->
  <h2>
    <a class="text" href={href}>{title}</a>
  </h2>

  <style>
    .title .text::before {
        content: "#";
        position: absolute;
        color: #bc8dd7;
        transform: translateX(-.8em);
        font-size: 90%
        
    }

    .title {
      line-height: 1.2;
      hyphens: none;
    }

    .title {
      display: flex;
      justify-content: center;
      text-align: center;
    }

    .title h2 {
      max-width: 90%;
    }


  /* 𖠁𖢫𖠄𖤕𖤍 𖢫𖤕 𖤓𖤂𖤍 ✶☆⚝🟂 ⛞ ⛞ ↈ 𒈨؆ 𒆙 ᨟ᨘᨗᨔ 𒐹𒐫𒀱𒀽
𖡤𖡴𖣘𖣐𖣔 𖢫𖢒𖢅𖠁𖠄𖡗𖡦𖡛 ⬢⬤⬟ ∇🗰 ▧▩▨ 𝍭

𝍤𝍡𝍣𝍢𝍠 𝍢𝍠𝍡𝍤𝍣  ⬠◇△ ꧂ ꧁ ༗ ░▒▓█

🐺

 */
  </style>
</div>
