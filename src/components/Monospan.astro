---
const content = await Astro.slots.render('default'); // string rendition of <slot />
---

<div class="monospan">
  {content.split("").map((c) => (
    <span>{c}</span>
  ))}
</div>

<style>
  .monospan {
    display: inline;
  }

  .monospan span {
    display: inline-block;
    width: 1.15ch;
    text-align: center;
  }
</style>