---
import Monospan from "../components/Monospan.astro";

const { selectedTag } = Astro.props;

function selected(s) {
  return selectedTag == s ? "selected" : "";
}
---

<div class="bar">
  <a class={selected("media")} href="/media">
    <Monospan>media</Monospan>
  </a>
  <a class={selected("research")} href="/research">
    <Monospan>research</Monospan>
  </a>
  <a class={selected("vault")} href="/vault">
    <Monospan>vault</Monospan>
  </a>
  <a class={selected("about")} href="/about">
    <Monospan>about</Monospan>
  </a>
</div>

<style>
  .bar {
    padding-bottom: 8px;
    padding-top: 8px;

    border-top: solid 2px #333;
    border-bottom: solid 4px #333;

    display: flex;
    flex-direction: row;
    flex-wrap: wrap;

    justify-content: space-around;

    line-height: 1;

    background-color: rgb(255, 255, 255);
    background-image: linear-gradient(
        135deg,
        rgba(0, 0, 0, 0.075) 0%,
        rgba(0, 0, 0, 0.075) 50%,
        transparent 50%,
        transparent 100%
      ),
      linear-gradient(
        45deg,
        transparent 0%,
        transparent 50%,
        rgba(0, 0, 0, 0.075) 50%,
        rgba(0, 0, 0, 0.075) 100%
      );
  }

  a {
    text-decoration: none;
    color: inherit;
    background-color: rgba(255, 255, 255, 0.523);
    padding: 3px;
  }

  a:nth-child(odd) {
    transform: skewX(-5deg);
  }

  a:nth-child(even) {
    transform: skewX(+5deg);
    transform: translateY(-1px);
  }

  a:nth-child(1) {
    border: thick solid rgb(255, 255, 0); /* Yellow */
  }

  a:nth-child(2) {
    border: thick solid rgb(0, 255, 0); /* Green */
  }

  a:nth-child(3) {
    border: thick solid rgb(255, 0, 255); /* Pink */
  }

  a:nth-child(4) {
    border: thick solid rgb(0, 255, 255); /* Blue */
  }

  .selected {
    font-weight: bold;
  }
</style>
